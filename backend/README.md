# Backend

FastAPI backend for the AI-08 AI FinOps Governance MVP.

Milestone 1 contains only the runnable application shell. APIs, business services, agents, and database setup are added in later milestones.

## Run

```bash
uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload
```
