# Frontend

React + TypeScript frontend for the AI-08 AI FinOps Governance MVP.

Milestone 1 contains only the runnable Vite shell. Pages, API integration, dashboard components, and charts are added in later milestones.

## Run

```bash
npm install
npm run dev -- --host 0.0.0.0
```
